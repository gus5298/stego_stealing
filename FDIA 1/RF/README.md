# 🧠 RF-Based Steganography on FDIA 1 Dataset

This repository contains a Jupyter Notebook that implements a Random Forest-based steganographic pipeline using the FDIA 1 dataset. The notebook has already been executed and includes all results.

---

## 📓 Notebook

- **`RF FDIA 1.ipynb`**  
  Contains:
  - Random Forest-based image classification
  - Steganographic message embedding and extraction
  - Evaluation using image quality metrics (PSNR, SSIM, MSE)
  - Classification results (confusion matrix, accuracy, etc.)

---

## 📊 Results Included

- ✅ Confusion matrix (with visualization)
- ✅ Classification accuracy
- ✅ Image quality metrics (MSE, PSNR, SSIM)
- ✅ Message decoding accuracy (if applicable)

All outputs are saved — no need to re-run the notebook to see results.

---

## 💾 Requirements

Install dependencies with:

```bash
pip install -r requirements.txt
