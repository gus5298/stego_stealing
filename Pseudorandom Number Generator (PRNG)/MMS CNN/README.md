# Project from `main.ipynb`

## Description
This project contains code extracted from a Jupyter notebook (`main.ipynb`). It includes various Python modules for data processing, visualization, or modeling (based on used libraries).

## How to Run

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Open the notebook:

```bash
jupyter notebook main.ipynb
```

## Requirements

See `requirements.txt` for all required Python packages.
